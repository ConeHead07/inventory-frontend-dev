import { ScannerinputDirective } from './scannerinput.directive';

describe('ScannerinputDirective', () => {
  it('should create an instance', () => {
    const directive = new ScannerinputDirective();
    expect(directive).toBeTruthy();
  });
});
